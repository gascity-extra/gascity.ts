# npm configuration for gascity.ts monorepo

# Use workspace protocol for local development
# This allows packages to reference each other with workspace:*
workspace=@gascity/*

# Registry configuration (optional, for private registry)
# registry=https://registry.npmjs.org/

# Authentication (DO NOT commit actual tokens)
# //registry.npmjs.org/:_authToken=${NPM_TOKEN}
