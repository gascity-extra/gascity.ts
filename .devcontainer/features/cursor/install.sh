#!/bin/bash

# Cursor CLI Installation Script

VERSION=${VERSION:-"latest"}

echo "Installing Cursor CLI (version: ${VERSION})..."

# Cursor CLI does not currently provide a supported non-interactive installer.
# The feature is kept as a placeholder; the devcontainer still comes up.
echo "Warning: Cursor CLI install is not implemented yet. Skipping." >&2
exit 1
